création du projet initial avec GIT en local
mkdir git-projet
cd git-projet
git init		pour initialiser GIT dnas le projet qui va être vide pour l'instant

enregistrement du fichier dans le projet
git add README.txt
git commint -m "msg changement+but"
